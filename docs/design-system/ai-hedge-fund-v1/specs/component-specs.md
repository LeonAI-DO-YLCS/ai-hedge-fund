# Component Specs

## Coverage

- Button
- Input
- Card
- Tabs
- Dialog
- Sidebar
- Table
- Badge
- FlowNode
- OutputPanel

## Component Behavior

### Button
- Anatomy: root, content, optional icon/label slot.
- States: default, hover, focus-visible, disabled, loading.
- Accessibility: keyboard reachable, visible focus, semantic role when interactive.
- Content rules: concise labels, avoid truncation for critical actions.

### Input
- Anatomy: root, content, optional icon/label slot.
- States: default, hover, focus-visible, disabled, loading.
- Accessibility: keyboard reachable, visible focus, semantic role when interactive.
- Content rules: concise labels, avoid truncation for critical actions.

### Card
- Anatomy: root, content, optional icon/label slot.
- States: default, hover, focus-visible, disabled, loading.
- Accessibility: keyboard reachable, visible focus, semantic role when interactive.
- Content rules: concise labels, avoid truncation for critical actions.

### Tabs
- Anatomy: root, content, optional icon/label slot.
- States: default, hover, focus-visible, disabled, loading.
- Accessibility: keyboard reachable, visible focus, semantic role when interactive.
- Content rules: concise labels, avoid truncation for critical actions.

### Dialog
- Anatomy: root, content, optional icon/label slot.
- States: default, hover, focus-visible, disabled, loading.
- Accessibility: keyboard reachable, visible focus, semantic role when interactive.
- Content rules: concise labels, avoid truncation for critical actions.

### Sidebar
- Anatomy: root, content, optional icon/label slot.
- States: default, hover, focus-visible, disabled, loading.
- Accessibility: keyboard reachable, visible focus, semantic role when interactive.
- Content rules: concise labels, avoid truncation for critical actions.

### Table
- Anatomy: root, content, optional icon/label slot.
- States: default, hover, focus-visible, disabled, loading.
- Accessibility: keyboard reachable, visible focus, semantic role when interactive.
- Content rules: concise labels, avoid truncation for critical actions.

### Badge
- Anatomy: root, content, optional icon/label slot.
- States: default, hover, focus-visible, disabled, loading.
- Accessibility: keyboard reachable, visible focus, semantic role when interactive.
- Content rules: concise labels, avoid truncation for critical actions.

### FlowNode
- Anatomy: root, content, optional icon/label slot.
- States: default, hover, focus-visible, disabled, loading.
- Accessibility: keyboard reachable, visible focus, semantic role when interactive.
- Content rules: concise labels, avoid truncation for critical actions.

### OutputPanel
- Anatomy: root, content, optional icon/label slot.
- States: default, hover, focus-visible, disabled, loading.
- Accessibility: keyboard reachable, visible focus, semantic role when interactive.
- Content rules: concise labels, avoid truncation for critical actions.

